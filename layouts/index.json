{{- /* Поисковый индекс.
       html — уже отрендеренная карточка: результаты выглядят ровно так же,
       как обычные карточки, без дублирования вёрстки в JS.
       content — сплющенный plain-text (без переводов строк и лишних пробелов),
       это заметно уменьшает JSON без потери полнотекстового поиска. */ -}}
{{- $index := slice -}}

{{- with site.GetPage "section" "posts" -}}
  {{- range .Sections -}}
    {{- if and (not .Draft) (gt (len .RegularPages) 0) -}}
      {{- $index = $index | append (dict
        "id" .RelPermalink
        "title" .Title
        "content" (replaceRE `\s+` " " (or .Plain .Description) | chomp)
        "tags" (.Params.tags | default slice)
        "html" (partial "ui/section-card.html" .)
      ) -}}
    {{- end -}}
  {{- end -}}
{{- end -}}

{{- range where (where site.RegularPages "Type" "posts") "Draft" false -}}
  {{- $index = $index | append (dict
    "id" .RelPermalink
    "title" .Title
    "content" (replaceRE `\s+` " " .Plain | chomp)
    "tags" (.Params.tags | default slice)
    "html" (.Render "summary")
  ) -}}
{{- end -}}

{{- $index | jsonify -}}
