---
# Курс. Создаётся так:
#   hugo new content posts/<курс>/_index.md --kind course
#
# Комментарии в конце строк — не декорация: по ним scripts/check-content.py
# понимает, что обязательно, что перечисление, а что число. Правишь схему —
# правь здесь, проверка подхватит сама.
#
# Одна папка = один ВАРИАНТ конспекта: преподаватель + автор + учебный год.
# Если по предмету уже есть чужой конспект — не смешивайте, заведите свою
# папку и поставьте одинаковый subject. На главной они склеятся в одну
# карточку с выбором.

title: "{{ replace .File.ContentBaseName `-` ` ` | title }}"   # required
description: ""

semester: 0                    # int
accent: 1                      # recommended  int: 1..6
weight: 0                      # порядок в списке и порядок вариантов внутри
                               # предмета; при нескольких вариантах обязателен

# Кто вёл и кто записывал.
teacher: ""                    # recommended-with: semester
author: ""                     # recommended  ник GitHub → аватарка и ссылка сами
                               # можно списком: authors: [notakeith, {name: ..., github: ...}]

academicYear:                  # recommended  2025 → «2025/26»; можно "2025/26"
                               # без него не работает пометка устаревших конспектов

# Ключ предмета. Заполняется, только если вариантов несколько.
# У всех вариантов должны совпадать title, accent и semester.
subject: ""
subjectTitle: ""               # если папки называются по-разному, а на карточке
                               # нужно одно название — задайте любому варианту

# Метка по умолчанию для всех лекций курса.
noteType: human                # enum: human, ai, ai-pro
aiModel: ""
aiSource: ""

abbr: ""                       # аббревиатура на карточке, иначе по инициалам
tag: ""                        # маленькая плашка на карточке

# ВНИМАНИЕ: draft у _index.md скрывает ВЕСЬ курс со всеми лекциями разом,
# даже если сами лекции не черновики. Убирайте перед PR.
draft: true
---